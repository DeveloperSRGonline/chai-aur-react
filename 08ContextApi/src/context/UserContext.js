import React from 'react'


// provider means giving access to all the compoents of the context just like global variable

export const UserContext = React.createContext();

export default UserContext;